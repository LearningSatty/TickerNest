// Root build file. The :core subproject is pure JVM Kotlin so it can be built
// and tested without the Android SDK. The :app subproject is an Android module
// (Compose) that depends on :core but isn't built here.
